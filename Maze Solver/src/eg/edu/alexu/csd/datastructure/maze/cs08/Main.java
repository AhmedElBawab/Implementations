package eg.edu.alexu.csd.datastructure.maze.cs08;

import java.io.File;

import java.io.IOException;

import javafx.application.Application;
import javafx.fxml.FXMLLoader;
import javafx.stage.Stage;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.layout.BorderPane;

public class Main /* extends Application */ {
	/*
	 * @Override public void start(Stage primaryStage) throws IOException {
	 * 
	 * Parent root = FXMLLoader.load(getClass().getResource("Gui.fxml")); Scene
	 * scene = new Scene(root,600,400);
	 * scene.getStylesheets().add(getClass().getResource("application.css").
	 * toExternalForm()); primaryStage.setScene(scene);
	 * primaryStage.setTitle("MAZE SOLVER APP ^_^"); primaryStage.show(); }
	 */
	public static void main(String[] args) {
		// launch(args);
	}
}
